<%= form_for (:session) do |f| %>
  <%= f.label :username, 'Enter your username' %>
  <%= f.text_field :username, autocomplete: 'off' %>
  <%= submit_tag 'Sign in' %>
  <%= submit_tag 'Register', :name => 'register_button' %>
<% end %>
<%= render partial: "layouts/notification" %>